/**
 * @orbit/shared
 *
 * Shared types and contracts between the frontend and backend.
 *
 * This package will eventually hold:
 *   - Task, Ring, Priority, Status types
 *   - Zod schemas for validation
 *   - API request/response contracts
 *   - Error codes
 *
 * For now, it is intentionally empty. Real types will be added
 * when the backend defines them in later chunks.
 */

export {};
