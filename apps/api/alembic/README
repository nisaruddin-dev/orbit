Alembic migration environment for the Orbit API.

Migrations are generated with:

    uv run alembic revision --autogenerate -m "description"

And applied with:

    uv run alembic upgrade head

The database URL is read from app.config.get_settings().database_url,
not from alembic.ini. See env.py for details.
