/**
 * @module data
 *
 * Server communication layer. API clients, React Query hooks,
 * mutation handlers, IndexedDB persistence, and sync orchestration.
 *
 * All network access flows through this module. Components never
 * call fetch() directly.
 *
 * Populated in Chunk 11 (Backend) and Chunk 12 (Local-First & Sync).
 */

export {};
