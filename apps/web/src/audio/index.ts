/**
 * @module audio
 *
 * Sound engine. Ambient bed, interaction tones, completion chime,
 * and mute state. Wraps Howler.js (or Web Audio API directly).
 *
 * Sound is a first-class material in Orbit, not an afterthought.
 * Every visual transition has a corresponding sound.
 *
 * Populated in Chunk 16 (Audio).
 */

export {};
