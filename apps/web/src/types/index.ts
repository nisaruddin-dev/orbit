/**
 * @module types
 *
 * TypeScript types specific to the frontend.
 *
 * Note: shared types between frontend and backend live in
 * @orbit/shared (packages/shared/src/). This module is for
 * frontend-only types.
 *
 * Populated as needed.
 */

export {};
