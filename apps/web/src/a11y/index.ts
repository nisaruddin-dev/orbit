/**
 * @module a11y
 *
 * Accessibility layer. Semantic HTML mirror of the 3D scene,
 * ARIA live regions, keyboard navigation, and focus management.
 *
 * The 3D scene is aria-hidden. Every task is mirrored as a semantic
 * element so screen readers can operate the app.
 *
 * Populated in Chunk 14 (Fallback and Recovery).
 */

export {};
